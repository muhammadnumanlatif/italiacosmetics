https://italiacosmetics.pages.dev/* https://italiacosmetics.com/:splat 301

# No explicit SPA fallback rule needed here: Cloudflare Pages already
# serves index.html by default for any path that doesn't match a real
# static file or an /api/* function (verified - even a nonexistent path
# returns the app shell, not a 404). An earlier "/* /index.html 200"
# rule here was rejected at deploy time as a potential infinite loop
# (see deployment build logs) and never actually took effect.
